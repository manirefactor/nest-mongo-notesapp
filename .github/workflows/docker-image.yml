name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
 
        
  Deploy:
    #needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Check Out Repo 
      uses: actions/checkout@v2
    - name: Pulling Docker Image To VM
      #uses: actions/checkout@v2
      run: |
            chmod +x  ./test.sh 
             ./test.sh 
      shell : bash

        
